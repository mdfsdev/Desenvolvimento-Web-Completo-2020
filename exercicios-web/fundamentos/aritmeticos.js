const [a, b, c, d] = [8, 5, 7, 2] // à esquerda operador destructuring
const soma = a + b + c + d
const subtracao = a - b
const multiplicacao = a * b
const divisao = a / b
const modulo = a % b // resto da divisão
//a++  sintaxe postfix: operando (a) + operador (++)
//++a  sintaxe prefix:  operador (++) + operando (a)
//a + b  sintaxe infix: operando (a) + operador (+) + operando (b). Neste caso, + é Operador binário
//+a Neste caso, + é Operador unário
//a + b + c Neste caso, + é Operador binário
// Operadores aritméticos são operadores binários (sobre dois operandos) (?) 